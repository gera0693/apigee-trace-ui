import { TraceAnalyzerComponent } from './pages/trace-analyzer/trace-analyzer';
import { Routes } from '@angular/router';

export const routes: Routes = [
  {
    path: '',
    component: TraceAnalyzerComponent
  }
];

